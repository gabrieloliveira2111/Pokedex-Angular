import { AtributtesPipe } from './atributtes.pipe';

describe('AtributtesPipe', () => {
  it('create an instance', () => {
    const pipe = new AtributtesPipe();
    expect(pipe).toBeTruthy();
  });
});
